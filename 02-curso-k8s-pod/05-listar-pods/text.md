## Listando pods

Assim como namespaces também podemos usar a versão curta, longa, singular e plural

Singular:

```bash
kubectl get pod
```{{}}

Plural:

```bash
kubectl get pods
```{{}}

Curto:

```bash
kubectl get po
```{{}}
