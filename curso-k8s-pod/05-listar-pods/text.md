## Listando pods

Assim como namespaces também podemos usar a versão curta, longa, singular e plural

Singular:

```plain
kubectl get pod
```{{}}

Plural:

```plain
kubectl get pods
```{{}}

Curto:

```plain
kubectl get po
```{{}}
